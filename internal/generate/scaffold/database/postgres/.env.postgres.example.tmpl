# Copy to .env.postgres (chmod 600). This file is sent only to PostgreSQL, not the API.
# Use three different random passwords of at least 20 bytes, e.g. openssl rand -hex 32.
# Do not source this file as shell code. Compose reads it as an env file.
POSTGRES_PASSWORD=
POSTGRES_MIGRATOR_PASSWORD=
POSTGRES_RUNTIME_PASSWORD=
