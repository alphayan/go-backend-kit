# {{.Module}}

本项目由 [go-backend-kit](https://github.com/alphayan/go-backend-kit) {{.Version}} 生成。

[English](README.md)

## 环境要求

- Go 1.26.4 或更高版本（Atlas v1.2.3 的最低要求）；推荐 Go 1.26.5 或更新的受支持补丁版本
- Docker，用于 PostgreSQL 开发环境和 Atlas 迁移

## 添加资源

```bash
go tool gobackend add path/to/resource.yaml
go tool gobackend check
```

`resources/` 中的 YAML 是唯一事实来源。纳入版本管理的 `.gobackend-generated.json` 清单记录生成器实际拥有的每条路径及其 SHA-256 摘要；普通手写 `.go` 文件永不覆盖。陈旧生成文件只有在当前内容仍与清单一致时才会删除；如果被人工修改，生成会安全报错，不会删除该文件。

`go tool gobackend generate` 会保留固定版本的官方 `go tool gorm gen` 工作流，重新生成 store 筛选和排序实际使用的字段辅助。`gormgen/query_gen.go` 只由官方 GORM CLI 生成，gobackend 不重写该输出。

## 数据库与迁移

```bash
docker compose up -d postgres
make migration name=create_products
DATABASE_URL='postgres://app:app@localhost:5432/app?sslmode=disable' make migrate-apply
```

迁移命令使用固定的开源 Atlas v1.2.3 Community Docker 镜像：先在宿主机运行生成的 GORM Provider，再让 Atlas 使用临时 PostgreSQL 开发库计算差异。Community 配置用于版本化迁移生成、应用和直接 schema 比较；高级迁移 lint、回滚、迁移测试、审批策略及高级数据库对象治理不在该配置范围内。应用前请审查 `migrations/` 中的 SQL。生产启动不会调用 `AutoMigrate`。

## 启动

```bash
cp .env.example .env
set -a; . ./.env; set +a
make run
```

可访问 `/api/v1`、`/health/live`、`/health/ready`、`/openapi.json` 和 `/docs`。

## 验证

```bash
go tool gobackend check
go test -race ./...
go vet ./...
go tool govulncheck ./...
```

SQLite 仅用于本地快速契约测试；CI 会先把已审查迁移应用到 PostgreSQL，再运行相同契约。

## 运行时默认值

PostgreSQL 连接池默认最多 25 个连接、25 个空闲连接，连接最长存活 30 分钟、最长空闲 5 分钟；可通过 `DB_MAX_OPEN_CONNS`、`DB_MAX_IDLE_CONNS`、`DB_CONN_MAX_LIFETIME` 和 `DB_CONN_MAX_IDLE_TIME` 配置。启动时会在 `DB_CONNECT_TIMEOUT`（默认 5 秒）内执行数据库探测，失败则拒绝启动；停机时显式关闭连接池。

## License

MIT
