env "local" {
  src = "file://.gobackend/schema.sql"
  dev = getenv("ATLAS_DEV_URL")

  migration {
    dir = "file://migrations"
  }
}

env "ci" {
  src = "file://.gobackend/schema.sql"
  url = getenv("DATABASE_URL")
  dev = getenv("ATLAS_DEV_URL")

  migration {
    dir = "file://migrations"
  }
}
